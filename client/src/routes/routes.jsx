import React from 'react';
import { Switch, BrowserRouter } from 'react-router-dom';

const MainRouter = () => (
  <BrowserRouter>
    <Switch>
      {/* Restricted Routes */}

      {/* Private Routes */}

      {/* Public Routes */}
    </Switch>
  </BrowserRouter>
);

export default MainRouter;
